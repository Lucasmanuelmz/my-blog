   <header>
   <nav class="container flex max-w-7xl">
        <a href="/">Início</a>
        <%if(categories && categories.length > 0) {%>
            <% categories.forEach(category => { %> 
                <a class="px-2" href="/category/<%= category.slug %>"><%= category.title %></a>
            <%})%>
        <%}%>
        <a href="/create/user" class="py-2 px-5 bg-purple-700 rounded-xl" type="submit">Criar conta</a>
        <a href="/login" class="py-2 px-5 bg-purple-700 rounded-xl" type="submit">Entrar</a>
    </nav>
   </header>